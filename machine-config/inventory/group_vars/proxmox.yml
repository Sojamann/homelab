---
# The cluster-wide tasks -- ACME account, DNS plugin, API user and token --
# must run exactly once. This names the node they run against. Any node in the
# cluster will do; it only has to be one of them, and it has to exist.
cluster_primary: titan
