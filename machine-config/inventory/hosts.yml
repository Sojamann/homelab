---
# Machines that do not exist yet stay commented out. Adding one here is half
# the job -- network/unifi_dns.tf carries the address again as an A record, and
# flux/infrastructure/configs/proxmox scrapes it.
all:
  children:
    proxmox:
      hosts:
        titan:     { ansible_host: 10.212.2.151 }
        # enceladus: { ansible_host: 10.212.2.152 }
        # mimas:     { ansible_host: 10.212.2.153 }
        # rhea:      { ansible_host: 10.212.2.154 }
        # iapetus:   { ansible_host: 10.212.2.155 }
        # dione:     { ansible_host: 10.212.2.156 }
